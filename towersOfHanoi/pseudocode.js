class TowersOfHanoi {
  run() {
    //Game starts with all pieces on first rod
    //Player moves one disk at a time
      //Player cannot move a larger disk on top of a smaller task
    //Game ends when all disks are placed in correct order on end rod
  }
}